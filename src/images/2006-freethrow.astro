---
import { Image, inferRemoteSize } from "astro:assets";
const file = "freethrow champ Hanna SChumaker WEst Linn 2006.jpg";
const alt = "2006 freethrow champ Hanna Schumaker, West Linn";
const { width, height } = await inferRemoteSize(
  `${import.meta.env.AWS}${file}`
);
---

<figure>
  <Image
    src=`${import.meta.env.AWS}${file}`
    alt={alt}
    width={width}
    height={height}
  />
  <figcaption>{alt}</figcaption>
</figure>
